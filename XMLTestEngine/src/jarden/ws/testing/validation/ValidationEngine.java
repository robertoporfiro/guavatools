package jarden.ws.testing.validation;

import jarden.ws.testing.results.ResultsSession;

import java.util.Collection;

import org.apache.xmlbeans.XmlObject;



public class ValidationEngine {
    
    /**
     * validates 1 response XML document against a set of validators
     * @param xml
     * @param session
     */
    public void validate(XmlObject xml, ResultsSession session, Collection<Validator> validators){
     
    }

}
